function duplicateCount(text){
    let repeatLetter = [], textArray = text.split(""), letterEquals, currentLetter, i;
    letterEquals = textArray.map(letter => {
        
    })
    return repeatLetter
  }

  console.log(duplicateCount('banana'));